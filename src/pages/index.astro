---
import Layout from '../layouts/Layout.astro';
import HomeHeader from '../components/home/HomeHeader.astro';
---

<Layout title="Portal Principal">
  <HomeHeader />

  <main class="min-h-screen pt-24 pb-12 px-4 flex flex-col items-center justify-center relative z-10 overflow-hidden">
    <!-- Background Elements -->
    <div class="absolute inset-0 z-[-1] overflow-hidden pointer-events-none">
      <div class="absolute top-[-10%] left-[-10%] w-[50%] h-[50%] bg-[#f8b134]/20 blur-[100px] rounded-full"></div>
      <div class="absolute bottom-[-10%] right-[-10%] w-[50%] h-[50%] bg-[#bf8418]/20 blur-[100px] rounded-full"></div>
      <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-full h-full bg-[url('/noise.svg')] opacity-20 mix-blend-overlay"></div>
    </div>

    <!-- Script de Redirección Automática (Client-Side) -->
    <script is:inline>
      (function() {
        try {
          const userStateRaw = localStorage.getItem('bartimeo:user');
          if (userStateRaw) {
            const userState = JSON.parse(userStateRaw);
            if (userState.isAuthenticated === 'true') {
              window.location.replace('/reto');
            }
          }
        } catch (e) {
          console.error('Error reading localStorage for auto-redirect', e);
        }
      })();
    </script>

    <div class="text-center mb-12 animate-fade-in-up">
      <h1 class="text-4xl md:text-6xl font-serif font-black mb-4 tracking-tight drop-shadow-lg text-white">
        ¿A dónde quieres ir?
      </h1>
      <p class="text-white/90 text-lg md:text-xl max-w-lg mx-auto leading-relaxed font-medium drop-shadow-md">
        Selecciona tu destino para continuar tu experiencia en la Parroquia Jesucristo Redentor.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-10 w-full max-w-4xl animate-fade-in-up" style="animation-delay: 0.2s;">
      
      <!-- BOTÓN EL RETO -->
      <a href="/reto" class="animate-bounce-soft group relative block w-full aspect-[4/3] rounded-[2rem] overflow-hidden shadow-[0_20px_50px_rgba(248,177,52,0.3)] transition-all duration-300 hover:-translate-y-4 hover:scale-105 active:scale-90 border-[3px] border-[#f8b134]/50 hover:border-[#f8b134]">
        <!-- Fondo Glassmorphism -->
        <div class="absolute inset-0 bg-gradient-to-br from-[#f8b134]/40 to-[#bf8418]/20 backdrop-blur-xl z-0"></div>
        <div class="absolute inset-0 bg-[url('/noise.svg')] opacity-20 mix-blend-overlay z-0"></div>
        
        <!-- Imagen Sutil de Fondo (Trofeo) -->
        <div class="absolute inset-0 z-0 flex items-center justify-center opacity-30 group-hover:opacity-50 transition-opacity duration-500 transform group-hover:scale-110">
          <img src="/trofeo.webp" alt="Trofeo" class="w-[80%] h-[80%] object-contain drop-shadow-2xl mix-blend-luminosity" />
        </div>
        
        <!-- Glow hover -->
        <div class="absolute inset-0 bg-gradient-to-tr from-[#f8b134]/0 via-[#f8b134]/20 to-[#f8b134]/40 opacity-0 group-hover:opacity-100 transition-opacity duration-300 z-0"></div>

        <div class="relative z-10 h-full flex items-center justify-center p-6 text-center">
          <h2 class="text-5xl md:text-7xl font-serif font-black text-white tracking-tight drop-shadow-[0_4px_8px_rgba(0,0,0,0.6)] transform group-hover:scale-110 transition-transform duration-300">
            El Reto
          </h2>
        </div>
      </a>

      <!-- BOTÓN EL RETIRO -->
      <a href="/retiro" class="animate-bounce-soft-delayed group relative block w-full aspect-[4/3] rounded-[2rem] overflow-hidden shadow-[0_20px_50px_rgba(255,255,255,0.2)] transition-all duration-300 hover:-translate-y-4 hover:scale-105 active:scale-90 border-[3px] border-white/40 hover:border-white">
        <!-- Fondo Glassmorphism -->
        <div class="absolute inset-0 bg-gradient-to-br from-white/30 to-white/10 backdrop-blur-xl z-0"></div>
        <div class="absolute inset-0 bg-[url('/noise.svg')] opacity-20 mix-blend-overlay z-0"></div>
        
        <!-- Imagen Sutil de Fondo (Sagrado Corazón) -->
        <div class="absolute inset-0 z-0 flex items-center justify-center opacity-30 group-hover:opacity-50 transition-opacity duration-500 transform group-hover:scale-110">
          <img src="/sagrado-coraz-n-de-jes-s-e1464966918869_c.webp" alt="Sagrado Corazón" class="w-[80%] h-[80%] object-contain drop-shadow-2xl mix-blend-luminosity rounded-full" />
        </div>
        
        <!-- Glow hover -->
        <div class="absolute inset-0 bg-gradient-to-tr from-white/0 via-white/20 to-white/40 opacity-0 group-hover:opacity-100 transition-opacity duration-300 z-0"></div>

        <div class="relative z-10 h-full flex items-center justify-center p-6 text-center">
          <h2 class="text-5xl md:text-7xl font-serif font-black text-white tracking-tight drop-shadow-[0_4px_8px_rgba(0,0,0,0.6)] transform group-hover:scale-110 transition-transform duration-300">
            El Retiro
          </h2>
        </div>
      </a>

    </div>
  </main>
</Layout>

<style>
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes bounceSoft {
    0%, 100% {
      transform: translateY(0) scale(1);
    }
    50% {
      transform: translateY(-8px) scale(1.02);
    }
  }

  .animate-fade-in-up {
    animation: fadeInUp 0.8s ease-out forwards;
    opacity: 0;
  }

  .animate-bounce-soft {
    animation: bounceSoft 3s ease-in-out infinite;
  }
  
  .animate-bounce-soft-delayed {
    animation: bounceSoft 3s ease-in-out infinite;
    animation-delay: 1.5s;
  }
</style>
